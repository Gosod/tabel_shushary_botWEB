# 🚀 Release Guide — Production Deploy

## 📦 Файлы для релиза:

```
release/
├── index.html              ← Web App (загрузить на GitHub)
├── telegram_bot.py         ← Бот (загрузить на сервер)
├── requirements.txt        ← Python зависимости
├── telegram-bot.service    ← Systemd service файл
├── deploy.sh              ← Автоматический деплой скрипт
└── RELEASE_GUIDE.md       ← Эта инструкция
```

---

## ⚙️ Шаг 1: Настройка перед деплоем

### 1.1 Обнови Web App URL в bot

Открой `telegram_bot.py` и найди:
```python
WEBAPP_URL = 'https://your-username.github.io/telegram-bot-webapp/'  # UPDATE THIS!
```

Замени на свой GitHub Pages URL.

### 1.2 Обнови админов (если нужно)

```python
ADMIN_IDS = [
    699229724,   # mchsman
    924261386,   # eugeneoldyard
]
```

### 1.3 Обнови токен в systemd service

Открой `telegram-bot.service` и замени:
```
Environment="TELEGRAM_BOT_TOKEN=YOUR_PRODUCTION_TOKEN_HERE"
```

На реальный токен **рабочего бота** (не тестового!).

---

## 📱 Шаг 2: Загрузи Web App на GitHub

```bash
# В репозитории telegram-bot-webapp
1. Открой GitHub → telegram-bot-webapp
2. Замени index.html на новый
3. Commit changes
4. Подожди 1-2 минуты пока GitHub Pages обновится
5. Проверь в браузере: https://твой-username.github.io/telegram-bot-webapp/
```

---

## 🖥️ Шаг 3: Деплой на сервер

### Автоматический деплой (рекомендую):

```bash
./deploy.sh
```

Скрипт сделает всё сам:
- Скопирует файлы на сервер
- Обновит systemd service
- Перезапустит бота
- Покажет статус

### Ручной деплой (если нужен контроль):

```bash
# 1. Копируем файлы
scp telegram_bot.py vpsbg:~/telegram_bot.py
scp telegram-bot.service vpsbg:~/telegram-bot.service

# 2. На сервере
ssh vpsbg

# 3. Останавливаем старого бота
sudo systemctl stop telegram-bot

# 4. Обновляем service файл
sudo mv ~/telegram-bot.service /etc/systemd/system/telegram-bot.service
sudo systemctl daemon-reload

# 5. Запускаем нового бота
sudo systemctl start telegram-bot

# 6. Проверяем статус
sudo systemctl status telegram-bot

# 7. Смотрим логи
sudo journalctl -u telegram-bot -f
```

---

## ✅ Шаг 4: Проверка работы

### В Telegram:

```
1. Найди своего РАБОЧЕГО бота (не тестового!)
2. /start
3. Нажми [📱 Открыть приложение]
4. Проверь:
   ✓ Приложение открылось
   ✓ Проекты загрузились
   ✓ Создание отчёта работает
   ✓ Статистика показывается
   ✓ (Админ) Управление проектами доступно
```

### На сервере:

```bash
# Проверь что бот запущен
ssh vpsbg "sudo systemctl status telegram-bot"

# Смотри логи в реальном времени
ssh vpsbg "sudo journalctl -u telegram-bot -f"

# Проверь что файлы на месте
ssh vpsbg "ls -lh /home/kostik/telegram_bot.py"
```

---

## 🔄 Обновление после релиза

Когда нужно обновить бота:

```bash
# 1. Внеси изменения в telegram_bot.py локально
# 2. Запусти деплой
./deploy.sh

# Или вручную
scp telegram_bot.py vpsbg:~/telegram_bot.py
ssh vpsbg "sudo systemctl restart telegram-bot"
```

Когда нужно обновить Web App:

```bash
# Просто замени index.html на GitHub
# Подожди 1-2 минуты
```

---

## 📊 Мониторинг

### Логи бота:
```bash
ssh vpsbg "sudo journalctl -u telegram-bot -f"
```

### Статус бота:
```bash
ssh vpsbg "sudo systemctl status telegram-bot"
```

### Перезапуск при проблемах:
```bash
ssh vpsbg "sudo systemctl restart telegram-bot"
```

---

## ⚠️ Важные моменты

**1. Токены:**
- ❌ НЕ используй тестового бота в продакшене
- ✅ Используй токен РАБОЧЕГО бота
- ✅ Токен хранится только в systemd service файле на сервере

**2. Данные:**
- Все данные (отчёты, пользователи, проекты) в `/home/kostik/*.json`
- При обновлении бота данные НЕ теряются
- Бэкап: `ssh vpsbg "cp *.json backup/"`

**3. Напоминания:**
- Работают автоматически в 16:50 МСК (пн-пт)
- Настраиваются в коде: `REMINDER_HOUR`, `REMINDER_MINUTE`

**4. Web App:**
- Хостится на GitHub Pages (бесплатно навсегда)
- Обновления применяются через 1-2 минуты после commit
- URL должен быть прописан в `telegram_bot.py`

---

## 🆘 Troubleshooting

**Бот не запускается:**
```bash
# Смотри детальные логи
ssh vpsbg "sudo journalctl -u telegram-bot -n 50 --no-pager"

# Проверь токен
ssh vpsbg "sudo systemctl cat telegram-bot | grep TELEGRAM"

# Проверь права
ssh vpsbg "ls -l /home/kostik/telegram_bot.py"
```

**Web App не открывается:**
- Проверь URL в браузере напрямую
- Проверь что URL правильный в `telegram_bot.py`
- Подожди 2-3 минуты после commit на GitHub

**Статистика не обновляется:**
- Это ожидаемо — данные загружаются при `/start`
- Перезайди через `/start` для обновления

---

## 📝 Checklist перед релизом

- [ ] Web App загружен на GitHub Pages
- [ ] URL обновлён в telegram_bot.py
- [ ] Токен продакшн бота прописан в telegram-bot.service
- [ ] Админы обновлены в ADMIN_IDS
- [ ] Сделан бэкап текущих данных
- [ ] Протестирован локально с тестовым ботом
- [ ] deploy.sh запущен успешно
- [ ] Бот отвечает в Telegram
- [ ] Web App открывается
- [ ] Создание отчёта работает
- [ ] Напоминания настроены

---

**Готово! 🎉**
